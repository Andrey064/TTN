<UserControl x:Class="TTNAppCore.UI.View.DriverDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TTNAppCore.UI.View"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <!--счет с 0-->
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Водитель"/>
        <TextBox Grid.Row="1" Text="{Binding Driver.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Content="Водительское удостоверение" Grid.Row="2"/>
        <TextBox Grid.Row="3" Text="{Binding Driver.DrivingLicense, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>


        
        <StackPanel Grid.Row="7" Orientation="Horizontal">
            <Button Content="Сохранить" Width="150" Margin="10"
                    Command="{Binding SaveCommand}">
            </Button>
            <Button Content="Удалить" Width="150" Margin="10"
                    Command="{Binding DeleteCommand}">
            </Button>
        </StackPanel>

    </Grid>


</UserControl>
